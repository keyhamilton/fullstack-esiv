const notFound = {
                erro: 404,
                message: "not found"
            }

const badRequest = {
                erro: 400,
                message: "bad request"
            }

export { notFound, badRequest}